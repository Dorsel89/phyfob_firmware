&uicr {
    nfct-pins-as-gpios;
};
/* NOTE: this file is NOT automatically applied by the build (verified: only
 * sysbuild/mcuboot.overlay gets picked up for this board/app layout) - it
 * has always been dead/inert. The supercap ADC config that used to be
 * sketched here (commented out) now lives directly in phyfob.dts instead,
 * where it actually takes effect. Kept only for reference/history. */
/*
/ {
	zephyr,user {
		io-channels = <&adc 4>;
		io-channel-names = "A0";
	};
};


&adc {
	#address-cells = <1>;
	#size-cells = <0>;
	channel@4 {
		reg = <4>;
		zephyr,gain = "ADC_GAIN_1_6";
		zephyr,reference = "ADC_REF_INTERNAL";
		zephyr,acquisition-time = <ADC_ACQ_TIME_DEFAULT>;
		zephyr,input-positive = <NRF_SAADC_VDD>;
		zephyr,resolution = <12>;
	};

};
*/